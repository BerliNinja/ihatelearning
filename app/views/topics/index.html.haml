%p#notice= notice
%h2 Listing Topics
%table
  %thead
    %tr
      %th Title
      %th Learned
      %th Description
      %th Subject
      %th{:colspan => "3"} Actions
  %tbody
    - @topics.each do |topic|
      %tr
        %td= topic.title
        %td= topic.learned
        %td= topic.description.html_safe
        %td= topic.subject
        %td
          %ul
            = link_to 'Show Topic', topic
            = link_to 'Edit Topic', edit_topic_path(topic)
            = link_to 'Destroy Topic', topic, method: :delete, data: { confirm: 'Are you sure?' }
%br/
%a.btn.btn-primary.btn-lg{:href => new_topic_path, :role => "button"} New Topic
